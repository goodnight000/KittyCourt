# Cat Judge Root Configuration
# This file is used by the monorepo's concurrently setup

# Server will load its own .env from server/ directory
# These are just fallbacks/overrides if needed

# Server runs on port 3000
PORT=3000

# Client dev server runs on port 5173 (handled by Vite)
VITE_API_URL=http://localhost:3000/api
